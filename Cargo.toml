[package]
name = "short_commands"
version = "0.1.0"
edition = "2024"

[dependencies]
serde = {version = "1.0", features = ["derive"]}
serde_json = "1.0"

[[bin]]
name = "short_commands"
path = "src/bin/main.rs"

[lib]
name = "short_commands"
path = "src/lib.rs"